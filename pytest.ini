[pytest]
# Default validation covers lecture_intel/ only (the active app code).
# backend/tests/ is archived (see README §目录说明) and NOT part of the
# default test run. To execute it manually:
#   python -m pytest backend/tests --rootdir backend --override-ini="pythonpath=."
pythonpath = lecture_intel
testpaths = lecture_intel/tests
