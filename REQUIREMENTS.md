# Recorder — 需求规格（整理自用户原始需求）

> 本文档是开发的"总 prompt"：所有设计与取舍以此为准。

## 一句话定位

一个**本地离线、双击即开**的 macOS 录音转文字 App：以**转写准确性**为根基，
之上提供"雅思口语教官"与"课堂录音"两个专业模式。

## 优先级（用户原话："重点在录音转文字准确性上，在这个基础上，另外两个才好弄"）

| 优先级 | 需求 |
|---|---|
| P0 | 转写准确性：中英混合识别、不丢词、不串段 |
| P0 | 双击直接打开的 App（不要每次终端启动） |
| P0 | 支持「电脑直接收音录音」与「上传音频文件」两种输入 |
| P1 | 雅思口语教官模式 |
| P1 | 课堂录音模式 |

## P0 — 通用转写（根基）

- 全程本地处理，不调用云 API（隐私 + 离线可用）。
- 中英混合（code-switching）对话必须都能识别，不偏科。
- **尊重原文**：转写结果不做任何"自动改写/纠正"。说错的就保留错的
  （这是与市面录音转文字 App 的核心差异——它们会自动把语法、句子"修好"）。
- 输入：内置麦克风/外接麦克风直接录音；或拖拽/选择音频文件
  （m4a/mp3/wav/webm/flac/aac/ogg/opus）。
- 输出：txt / md / srt / json，带时间戳。

## P1 — 雅思口语教官模式

场景：用户与教官（助教）对话练习雅思口语，中英混合，环境中可能有
其他人交谈。

1. **说话人分离**：判断谁是教官、谁是答题者；环境中其他人的对话要排除。
   - 启发式：说英文长段回答的是答题者；提问/打断/中文讲解多的是教官。
   - 最终在界面上可手动对调两个角色。
2. **保留原文**：答题者的转写绝不自动纠正——错就是错，这是诊断的原材料。
3. **读音问题识别**（普通转写 App 做不到的核心点）：
   - 利用 whisper 的逐词置信度：置信度异常低的词 = 疑似发音不清/读错。
   - 标注出来供用户核对，不武断下结论。
4. **语法/句子/表达问题识别**：
   - 语法错误（时态、主谓一致、冠词等）；
   - 句子结构问题；
   - 表达不地道、不自然（中式英语）。
   - 本地规则 + LanguageTool（若本机已启动）实现，全部离线。
5. **教官的纠正**：教官现场给出的正确说法，识别后作为参考一并展示。
6. **模板化反馈报告**：按雅思四项评分维度（流利度连贯性 / 词汇 / 语法 /
   发音）汇总问题清单与修改建议。报告是结果，不是重点；重点是 3、4 的识别。

## P1 — 课堂录音模式

场景：空旷教室，有回声、底噪；老师声音可能远；周围同学可能闲聊。

1. **降噪增强**：DeepFilterNet 局部降噪（可开关；信噪比足够好时自动跳过）。
2. **排除非主讲人**：以主讲人（说话时长最长者）为主体，旁人闲聊段落剔除。
3. **术语**：课程术语词典纠错（仅课堂模式启用，且只改术语拼写不改句子）。
4. 长录音（1-2 小时）要能稳定跑完，进度可见。

## App 形态

- macOS 桌面应用（PySide6），**双击 .app 即启动**，不需要开终端。
- 界面：录音 Tab + 文件 Tab → 选模式（通用/课堂/雅思）→ 处理 → 结果面板
  （转写文本 + 雅思模式下的标注报告）→ 导出。
- 处理中显示分步进度，可取消。
- M5 / 16GB 内存的 MacBook 上流畅运行：ASR 用 Apple Silicon 加速
  （mlx-whisper），1 小时课堂录音 ≤ 10 分钟左右处理完。
- 可选 Ollama 大模型增强：16GB 上默认 7B 级（亚洲 Qwen / 欧洲 Mistral），
  与 Whisper **串行**；完整档位表见
  [lecture_intel/docs/LLM_MODELS.md](lecture_intel/docs/LLM_MODELS.md)。

## 技术决策（实现者笔记）

- 以 `lecture_intel/` 为唯一代码主体（GUI、流水线齐全）；`backend/`+
  `frontend/` 的 FastAPI 雅思批改是早期原型，其分析逻辑（发音置信度、
  语法规则、报告模板）移植进主体后归档。
- 准确性问题根因（旧版"效果不好"的来源）：
  1. venv 因项目目录改名（recoder→recorder）整体失效，很多依赖实际跑不起来；
  2. 自切 30s VAD 段逐段送 whisper：边界丢词、重叠段重复、上下文断裂；
     → 改为整文件交给 whisper（其内部带 VAD），只在超长音频时按静音分大块。
  3. initial_prompt 写死了机器学习术语，污染通用/雅思场景 → 按模式定制。
  4. LLM"纠错"会改写原文，违背"尊重原文"原则 → 仅课堂模式可选，
     雅思模式强制关闭。
- 说话人分离不用 pyannote（需要 HuggingFace token、模型 gated），
  改用免 token 的说话人向量（ECAPA/Resemblyzer）+ 聚类，2 人场景足够。
- 打包不用 py2app/PyInstaller 塞 torch（体积巨大且脆弱）：
  做一个轻量 .app 启动器，指向项目 venv 运行 `app.py`，
  双击体验一致且依赖可正常升级。
