[pytest]
# Main app lives in lecture_intel/ (the Recorder app). The legacy backend/
# FastAPI prototype keeps its own tests under backend/tests if ever needed.
pythonpath = lecture_intel
testpaths = lecture_intel/tests
